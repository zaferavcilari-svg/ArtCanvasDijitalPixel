import { initCanvas } from './canvas.js';
import { initPalette } from './palette.js';

// Oyunu başlat
initCanvas();
initPalette();

// 1 Saniyede bir senkronizasyon simülasyonu
setInterval(() => {
    // Burada pixelData üzerinden sunucudaki yeni pikseller kontrol edilecek
    console.log("Art Canvas Senkronize Ediliyor...");
}, 1000);
